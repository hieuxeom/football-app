import {TeamRole} from "@/utils/interfaces/teams/teams";

export const addPlayerToTeam = async (teamId: number | string, playerId: number | string) => {
    return;
}

export const isPlayerExistInTeam = async (teamId: number | string, playerId: number | string)=> {
    return;
}

export const updatePlayerInfo = (playerId: string | number) => {
    return;
}

export const getAllTeamStaffs = async (teamId: string | number) => {
    return;
}

export const setPlayerRoleInTeam = async (teamId: string | number, playerId: string | number, role: TeamRole) => {
    return;
}

